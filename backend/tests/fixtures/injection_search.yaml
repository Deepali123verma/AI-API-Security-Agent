openapi: 3.0.3
info:
  title: Injection Search API
  version: 1.0.0

paths:
  /items:
    get:
      summary: Search items
      parameters:
        - name: search
          in: query
          schema:
            type: string
        - name: id
          in: query
          schema:
            type: integer
      responses:
        '200':
          description: OK
