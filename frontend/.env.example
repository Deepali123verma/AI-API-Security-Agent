# Local Vite development: point at the FastAPI process.
VITE_API_BASE_URL=http://localhost:8000

# Vercel / production Docker builds: leave empty (or omit) so the browser
# calls the same public origin that serves the SPA + API.
# VITE_API_BASE_URL=
